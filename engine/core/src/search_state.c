#include <stdio.h>
#include "search_state.h"

/* Placeholder, we may want to dump search state for debugging/optimisation. */
void print_search_state(const search_state_t* search_state) {
    printf("search state\n");
}